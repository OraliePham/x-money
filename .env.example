# Default target. Safe public demo app for first run.
BASE_URL=https://demo.playwright.dev/todomvc

# Use true only during local debugging.
PWDEBUG=0

# Example credentials for your own test system. Do not commit real secrets.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
